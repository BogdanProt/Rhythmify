
<br />
<br />
<style>
    body {
        margin-bottom: 60px;
        background-color: #a29de3;
        background-size: 100%;
    }
</style>
<div class="row">
    @foreach (var user in ViewBag.UsersList)
    {
        <div class="col-4 mb-4">
            <div class="card" style="height:100%; width: 100%;">

                <div class="card-body d-flex justify-content-between flex-column">
                    <div>
                        <p><i class="bi bi-person-fill"> </i> <b>User Name:</b> @user.UserName</p>

                        <p><i class="bi bi-envelope"></i> <b>Email:</b> @user.Email</p>


                        @if (@user.DisplayName != null)
                        {
                            <p><i class="bi bi-person-fill"></i> <b>Display Name:</b> @user.DisplayName</p>
                        }
                    </div>
                    <div class="d-flex flex-row justify-content-between p-3">

                        <div> <a class="btn btn-sm btn-success" href="/Users/Edit/@user.Id"> Editare utilizator</a> </div>

                        <div> <a class="btn btn-sm btn-success" href="/Users/Show/@user.Id"> Vizualizare utilizator</a> </div>

                    </div>
                </div>

            </div>
        </div>
    }

</div>

<br />
<br />
